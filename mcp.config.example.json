{
  "_comment": "Each person adds this to their own MCP client config. CAMBIUM_AGENT_ID must be unique per person; CAMBIUM_REPO points at their own clone of the shared project. CAMBIUM_ORG_REPO (optional) points at their clone of the dedicated org knowledge repo — required only to read/write org scope.",

  "mcpServers": {
    "cambium": {
      "command": "python3",
      "args": ["/abs/path/to/cambium/cambium_server.py"],
      "env": {
        "CAMBIUM_REPO": "/abs/path/to/your/project-clone",
        "CAMBIUM_AGENT_ID": "jonny",

        "_board": "Which repo holds the agentsync coordination board distill reads. Set this explicitly and use the SAME value in the agentsync server's config -- the board is a shared team artifact, so its address must not follow the session's project. Omit it only when CAMBIUM_REPO itself holds the agentsync branch.",
        "AGENTSYNC_BOARD_REPO": "/abs/path/to/the/clone-holding-the-board",

        "_optional": "defaults shown below; omit unless you need to change them",
        "CAMBIUM_REMOTE": "origin",
        "CAMBIUM_TEAM_BRANCH": "cambium",
        "CAMBIUM_AGENTSYNC_BRANCH": "agentsync",
        "CAMBIUM_PROMOTE_RECALLS": "3",

        "_org": "org scope: a dedicated knowledge repo everyone clones",
        "CAMBIUM_ORG_REPO": "/abs/path/to/your/org-knowledge-clone",
        "CAMBIUM_ORG_PR": "1"
      }
    }
  }
}
